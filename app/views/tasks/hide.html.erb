<h1>非表示タスク一覧</h1>

<% if @tasks.any? %>
  <ul>
    <% @tasks.each do |t| %>
      <% if t.is_display == false %>
        <li><%= link_to t.title, t %> <%= t.created_at.strftime("%Y年%m月%d日") %></li>
      <% end %>
    <% end %>
  </ul>
<% end %>

<%= link_to "一覧", tasks_path %>